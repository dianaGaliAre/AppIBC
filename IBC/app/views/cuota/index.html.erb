<%= render '/computacion/head' %>
			<h1>Cuota</h1>
			<table class="table">
			<% @alumno.each do |c| %>
				<tr>
				<td><%= c.alumno.nombre_a %></td>
				<td><%= c.cantidad %></td>
				</tr>
			<% end %>
			</table>
		</div>
	</div>
</div>